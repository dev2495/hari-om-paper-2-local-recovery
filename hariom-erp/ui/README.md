# Legacy UI Folder

This `ui/` directory is legacy and is not used by the active ERP runtime.

The single canonical frontend is:
- `/Users/devarshthakkar/Documents/Hari Om Paper/apps/web-ui`

The active API gateway used by the frontend is:
- `/Users/devarshthakkar/Documents/Hari Om Paper/apps/bff-api`

Do not build new features in `ui/`. Keep all frontend updates in `apps/web-ui`.
